# Nexus Protocol - Secrets Template
# IMPORTANT: This is a TEMPLATE file. Do NOT commit actual secrets.
# Use kubectl create secret or external secrets management.
#
# Example:
#   kubectl create secret generic nexus-api-secrets \
#     --from-literal=DB_PASSWORD=your-password \
#     --from-literal=REDIS_PASSWORD=your-redis-password \
#     --from-literal=API_KEY=your-api-key \
#     -n nexus-protocol

apiVersion: v1
kind: Secret
metadata:
  name: nexus-api-secrets
  namespace: nexus-protocol
  labels:
    app.kubernetes.io/name: nexus-api
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  # Database credentials
  DB_USER: "nexus"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # Redis credentials
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # API authentication
  API_KEY: "CHANGE_ME_IN_PRODUCTION"
  
  # JWT secret for token signing (if used)
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
